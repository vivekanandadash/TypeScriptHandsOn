var tiger:string="animal";
console.log(tiger);

// how to configure a tsc file
//step 1 -> use - tsc --init
//step-2 -> use - tsc ---- to generate essential config  file now you no need to genrate ,js file manually
